tags:
  - me
parameters:
  - name: 'make profile'
    in: body
    required: True
    schema:
      id: NewUserData
      type: object
      properties:
        cognitoId:
          $ref: '#/definitions/UUID'
        vendorType:
          $ref: '#/definitions/VendorType'
      required:
        - cognitoId
        - vendorType
responses:
  201:
    description: Successfully created new user
    schema:
      type: boolean
  400:
    description: Failed to make user
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Bad request
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal server error
    schema:
      $ref: '#/definitions/Error'
  
